type: object
required:
  - employee_id
properties:
  employee_id:
    type: string
    example: "JIN0001"
    description: "社員番号"
  emergency_contacts:
    type: object
    required:
      - id
    properties:
      id:
        type: string
        example: "1"
        description: "緊急連絡先ID<br>従業員管理＞基本情報＞緊急連絡先で表示されるNo.（連番）です。"
