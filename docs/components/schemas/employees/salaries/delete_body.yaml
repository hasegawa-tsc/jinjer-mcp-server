type: object
required:
  - employee_id
properties:
  employee_id:
    type: string
    example: "JIN0001"
    description: "社員番号"
  salaries:
    type: object
    required:
      - id
    properties:
      id:
        type: string
        example: "1"
        description: "給与単価ID<br>従業員管理＞給与計算＞給与単価で表示されるNo.（連番）です。"
